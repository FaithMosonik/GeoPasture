import 'package:flutter_test/flutter_test.dart';

void main() {
  // Widget smoke tests require the full service graph (AppDatabase, Firebase,
  // Provider). End-to-end coverage lives in integration_test/.
  test('placeholder — passes until integration tests are wired up', () {});
}
